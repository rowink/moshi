# Minimal Netlify config for moshi
# Build is auto-detected by Netlify (pnpm-lock.yaml -> pnpm build, dist -> publish dir)

# For router history mode, ensure all paths land on index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200